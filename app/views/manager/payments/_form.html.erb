<%= form_for ([:manager, @project, @payment]) do |f| %>
<%= f.label :Enter_Amount %>
<%= f.text_field :amount, class:'form-control', style:'margin-top:10px' %>
<%= f.submit "Add Payment", style:'margin-top:20px' %>
<% end %>
